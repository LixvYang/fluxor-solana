[package]
name = "fluxor_solana"
version = "0.1.0"
description = "Fluxor on Solana paid Markdown article program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "fluxor_solana"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = "1.0.0"
solana-sha256-hasher = { version = "3.1.0", features = ["sha2"] }

[dev-dependencies]
litesvm = "0.10.0"
solana-account = "3.4.0"
solana-keypair = "3.1.2"
solana-message = "3.1.0"
solana-rent = "3.1.0"
solana-sdk-ids = "3.1.0"
solana-signer = "3.0.0"
solana-system-interface = { version = "3.2.0", features = ["bincode"] }
solana-transaction = "3.1.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
